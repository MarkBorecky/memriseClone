version: '2'
services:
  memriseclone-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/memriseclone-mongodb-config --bind_ip 0.0.0.0
  memriseclone-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  memriseclone-mongodb-config:
    image: mongo:4.2.7
    container_name: memriseclone-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
